# ADR 001: Single grounded diagnosis pipeline

Status: Accepted

Use one explicit retrieve → reason → validate workflow. Multiple agents would add latency and failure modes without improving this one-day demonstration. Trace events expose each boundary without pretending internal chain-of-thought is an audit log.
