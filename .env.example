# Optional. Leave empty for the deterministic offline demo.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Update these when changing models; used only for the UI estimate.
OPENAI_INPUT_USD_PER_M=0.15
OPENAI_OUTPUT_USD_PER_M=0.60
